Customer segmentation is generated within the project to simulate an upstream CRM or customer-data process. 
In a production environment, these segment assignments would typically be supplied by an existing segmentation pipeline or customer data platform.

10000 customers were generated with payment and rental details. The original data only had 599 customers. 18-20 years was added to the dates for the 599 original customers
to bring them up to date with generated customers.

The application supports configurable OpenAI models. During development, GPT-5.4 mini was used to minimize cost, while GPT-5.5 can be substituted for higher-quality analysis.
